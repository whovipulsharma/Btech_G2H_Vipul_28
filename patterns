#include <stdio.h> 
int main() 
{ int x=4, a=0,k=1,m=1,n; 
for(int i=0;i<x;i++) 
{a=0; m=1;
for(int j=0;j<2*x-1;j++) 
{ 
    if(a>=k)
    break;
    else if((i+j)>=x-1)
    {
        if(j<=(2*x-1)/2)
        {
        printf("%d",m);
        n=m;
        m++;
        }
        else
        {
                    printf("%d",--n);
        }
        a++; 
    }
    else
    printf(" ");
    } 
    k+=2; 
    printf("\n"); 
    } 
}




#include <stdio.h>
int main()
{
    int x=4;
   
 for(int i=0;i<x;i++)
 {
     for(int j=0;j<=i;j++)
     {
              if((i+j)%2==0)
              printf("A ");
              else
              printf("B ");
     }
     
     printf("\n");
 }
}
