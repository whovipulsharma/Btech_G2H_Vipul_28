#include <stdio.h>
int main() {
    int n,p=1;
    printf("Enter the number to find factorial: ");
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        p*=i;
    }
    printf("Factorial:%d",p);
    return 0;
}
