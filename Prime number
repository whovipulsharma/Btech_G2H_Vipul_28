#include <stdio.h>
int main() {
    int n,f=0;
    printf("Enter the number: ");
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        if(n%i==0)
        f++;
    }
    if(f==2)
    printf("Entered number is prime. ");
    else
    printf("Entered number is not prime. ");
    return 0;
}
