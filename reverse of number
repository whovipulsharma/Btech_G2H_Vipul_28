#include <stdio.h>
int main() {
    int n,s=0;
    printf("Enter the number: ");
    scanf("%d",&n);
    while(n!=0)
    {
        int d=n%10;
        s=s*10+d;
        n/=10;
    }
    printf("Sum of the digit of number: %d ",s);
    return 0;
}
