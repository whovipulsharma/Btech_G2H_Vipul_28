#include <stdio.h>
int main() {
    char ch[20];
    printf("Enter the string: ");
    scanf(" %s",ch);
    int n=0;
    while(ch[n]!='\0')
    {
        n++;
    }
printf("length of string: %d",n);
    return 0;
}
