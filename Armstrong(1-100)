#include <stdio.h>
#include<math.h>
int main() {
    int n,s=0;
    for(int i=1;i<=100;i++)
    {
       n=i;
       int x=log10(i)+1;
       s=0;
        while(n!=0)
    {
        int d=n%10;
        s+=pow(d,x);
        n/=10;
    }
    if(s==i)
    printf("%d ",i);
    }
    return 0;
}
