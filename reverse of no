// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n,rev=0;
    printf("Enter the number: ");
    scanf("%d",&n);
   while(n!=0)
   {
       int d=n%10;
       rev=rev*10+d;
       n/=10;
   }
   printf("Reverse of a number: %d",rev);
    return 0;
}
